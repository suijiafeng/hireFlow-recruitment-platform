{
  "name": "hireflow",
  "version": "0.1.0",
  "private": true,
  "description": "HireFlow - AI-Powered Recruitment Platform | 智能招聘 AI 辅助平台",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/web",
    "build:shared": "npm run build -w packages/shared",
    "dev": "npm run build:shared && npm run db:setup && concurrently -n db,shared,api,web -c yellow,cyan,green,magenta \"npm run db:serve -w apps/api\" \"npm run dev -w packages/shared\" \"npm run dev:wait -w apps/api\" \"npm run dev -w apps/web\"",
    "dev:api": "npm run build:shared && npm run db:setup && concurrently -n db,api -c yellow,green \"npm run db:serve -w apps/api\" \"npm run dev:wait -w apps/api\"",
    "dev:web": "npm run dev -w apps/web",
    "dev:external-db": "npm run build:shared && concurrently -n shared,api,web -c cyan,green,magenta \"npm run dev -w packages/shared\" \"npm run start:dev -w apps/api\" \"npm run dev -w apps/web\"",
    "db:setup": "npm run db:setup -w apps/api",
    "db:reset": "npm run db:reset -w apps/api",
    "db:import": "npm run db:import -w apps/api --",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:migrate": "npm run db:migrate -w apps/api",
    "db:generate": "npm run db:generate -w apps/api",
    "db:seed": "npm run db:seed -w apps/api",
    "format": "prettier --write \"{apps,packages}/*/src/**/*.{ts,tsx}\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "prettier": "^3.9.5"
  }
}
