# PostgreSQL 连接（与根目录 docker-compose.yml 对应）
DATABASE_URL="postgresql://arthr:arthr_dev_password@localhost:5432/arthr?schema=public"

# JWT 签名密钥（生产环境务必更换为强随机值）
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="12h"

# 服务端口
PORT=3000
