node_modules/
dist/
build/
*.tsbuildinfo

# prisma 生成的客户端（克隆后先执行 npm run db:generate）
apps/api/src/generated/

.env
.env.local
.env.*.local

# 本地 PGlite 数据目录（npm run dev 自动创建；要重来就 npm run db:reset）
apps/api/.pgdata/
# 迁移前从旧库导出的备份
.backup/

*.log
.DS_Store
coverage/
.vite/
.claude
